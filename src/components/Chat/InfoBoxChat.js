import classNames from "classnames/bind"
import styles from "./chat.module.css"
const cx = classNames.bind(styles)

function InfoBoxChat() {
  return <div className={cx("infobox-wrap")}>

  </div>
}

export default InfoBoxChat